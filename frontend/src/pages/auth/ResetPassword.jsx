import { useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import authService from "../../services/authService";

export default function ResetPassword() { const {token}=useParams(); const navigate=useNavigate(); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); const submit=async e=>{e.preventDefault();try{await authService.resetPassword(token,password);navigate("/login");}catch(error){setMessage(error.message);}}; return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm"><h1 className="text-2xl font-bold text-[#102A43]">Set a new password</h1><p className="mt-2 text-sm text-slate-500">Your email verification code is being used securely.</p>{message&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{message}</p>}<input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 h-12 w-full rounded-xl border border-slate-200 px-4" placeholder="At least 6 characters"/><button className="mt-4 h-12 w-full rounded-xl bg-orange-500 font-semibold text-white">Update password</button><Link to="/login" className="mt-5 block text-center text-sm font-semibold text-orange-500">Back to login</Link></form></main>; }
