import { useState } from "react";
import { Link } from "react-router-dom";
import authService from "../../services/authService";

export default function ForgotPassword() { const [email,setEmail]=useState(""); const [message,setMessage]=useState(""); const submit=async e=>{e.preventDefault();try{const r=await authService.forgotPassword(email);setMessage(r?.data?.resetUrl?`A verification code was sent. Development reset link: ${r.data.resetUrl}`:"If the account exists, a verification code has been sent.");}catch(error){setMessage(error.message);}}; return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm"><h1 className="text-2xl font-bold text-[#102A43]">Forgot password?</h1><p className="mt-2 text-sm text-slate-500">Enter your email. We’ll email you a one-time verification code.</p>{message&&<p className="mt-5 break-words rounded-lg bg-orange-50 p-3 text-sm text-orange-700">{message}</p>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-6 h-12 w-full rounded-xl border border-slate-200 px-4" placeholder="you@example.com"/><button className="mt-4 h-12 w-full rounded-xl bg-orange-500 font-semibold text-white">Send verification code</button><Link to="/login" className="mt-5 block text-center text-sm font-semibold text-orange-500">Back to login</Link></form></main>; }
