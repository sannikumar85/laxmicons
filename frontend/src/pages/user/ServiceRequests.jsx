import { useEffect, useMemo, useState } from "react";
import { FiSearch, FiCalendar, FiMapPin, FiClock, FiEye, FiPlus } from "react-icons/fi";
import { Link } from "react-router-dom";
import serviceRequestService from "../../services/serviceRequestService";

export default function ServiceRequests() {
  const [requests, setRequests] = useState([]); const [search, setSearch] = useState(""); const [status, setStatus] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { serviceRequestService.getMyServiceRequests().then((response) => setRequests(response?.data?.requests || [])).catch((requestError) => setError(requestError.message || "Unable to load your requests.")).finally(() => setLoading(false)); }, []);
  const filtered = useMemo(() => requests.filter((request) => `${request.service} ${request.location} ${request.description}`.toLowerCase().includes(search.toLowerCase()) && (!status || request.status === status)), [requests, search, status]);
  const cancel = async (id) => { try { await serviceRequestService.cancelServiceRequest(id); setRequests((previous) => previous.map((request) => request._id === id ? { ...request, status: "cancelled" } : request)); } catch (requestError) { setError(requestError.message); } };
  return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold text-[#E87524]">Dashboard</p><h1 className="mt-1 text-2xl font-bold text-[#102A43]">Service Requests</h1><p className="mt-2 text-sm text-gray-500">Only requests submitted from your account appear here.</p></div><Link to="/dashboard/service-requests/create" className="inline-flex h-11 items-center justify-center gap-2 rounded-xl bg-[#E87524] px-5 text-sm font-semibold text-white hover:bg-[#d9681b]"><FiPlus/>New Request</Link></div><div className="flex flex-col gap-3 rounded-2xl border border-gray-100 bg-white p-4 shadow-sm md:flex-row"><div className="relative flex-1"><FiSearch size={17} className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"/><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search your requests..." className="h-11 w-full rounded-xl border border-gray-200 bg-gray-50 pl-11 pr-4 text-sm"/></div><select value={status} onChange={(event) => setStatus(event.target.value)} className="h-11 rounded-xl border border-gray-200 bg-gray-50 px-4 text-sm"><option value="">All statuses</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="in-progress">In progress</option><option value="completed">Completed</option><option value="rejected">Rejected</option><option value="cancelled">Cancelled</option></select></div>{error && <div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="space-y-4">{loading ? <div className="rounded-2xl bg-white p-10 text-center text-sm text-gray-500">Loading your requests...</div> : filtered.map((request) => <article key={request._id} className="rounded-2xl border border-gray-100 bg-white p-5 shadow-sm"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex gap-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-orange-50 text-[#E87524]"><FiClock size={20}/></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-bold text-[#102A43]">{request.service}</h3><span className="text-xs text-gray-400">{request._id}</span></div><p className="mt-2 text-sm text-gray-600">{request.description}</p><div className="mt-3 flex flex-wrap gap-4 text-xs text-gray-500"><span className="flex items-center gap-1.5"><FiMapPin/>{request.location || "Location pending"}</span><span className="flex items-center gap-1.5"><FiCalendar/>{request.createdAt ? new Date(request.createdAt).toLocaleDateString("en-IN") : "Recently"}</span></div></div></div><div className="flex items-center gap-3"><Status status={request.status}/>{request.status === "pending" && <button type="button" onClick={() => cancel(request._id)} className="rounded-lg border border-red-200 px-3 py-2 text-xs font-semibold text-red-500 hover:bg-red-50">Cancel</button>}<button type="button" className="flex h-10 items-center gap-2 rounded-lg border border-gray-200 px-4 text-xs font-semibold text-gray-600"><FiEye/>View</button></div></div></article>)}{!loading && !filtered.length && <div className="rounded-2xl border border-gray-100 bg-white p-10 text-center text-sm text-gray-500">You have not submitted any matching requests.</div>}</div></div>;
}

function Status({ status }) { const labels = { pending: "Pending", approved: "Approved", "in-progress": "In progress", completed: "Completed", rejected: "Rejected", cancelled: "Cancelled" }; const styles = { pending: "bg-blue-50 text-blue-700", approved: "bg-indigo-50 text-indigo-700", "in-progress": "bg-orange-50 text-orange-700", completed: "bg-green-50 text-green-700", rejected: "bg-red-50 text-red-700", cancelled: "bg-gray-100 text-gray-600" }; return <span className={`rounded-full px-3 py-1.5 text-xs font-semibold ${styles[status] || "bg-gray-50 text-gray-600"}`}>{labels[status] || status}</span>; }
