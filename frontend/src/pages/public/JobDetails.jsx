import { useEffect, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import jobService from "../../services/jobService";
import JobDetailsView from "../../components/jobs/JobDetails";
import JobApplicationForm from "../../components/jobs/JobApplicationForm";
import { useAuth } from "../../hooks/useAuth";
export default function JobDetails() { const {id}=useParams(); const navigate=useNavigate(); const { isAuthenticated } = useAuth(); const [job,setJob]=useState(null); const [showForm,setShowForm]=useState(false); const [message,setMessage]=useState(""); useEffect(()=>{jobService.getJobById(id).then(r=>setJob(r?.data?.job)).catch(()=>setJob(null));},[id]); const apply = async (data) => { if (!isAuthenticated) return navigate("/login", { state: { from: `/jobs/${id}` } }); const formData = new FormData(); formData.append("applicantName", data.name); formData.append("phone", data.phone); formData.append("coverLetter", data.coverLetter); formData.append("resume", data.resume); try { await jobService.applyForJobWithResume(id, formData); setMessage("Application submitted successfully."); setShowForm(false); } catch (error) { setMessage(error.message || "Unable to submit application."); } }; return <div className="mx-auto max-w-7xl px-4 pb-16 pt-32 sm:px-6"><JobDetailsView job={job} onApply={() => setShowForm(true)}/>{message && <p className="mt-5 rounded-xl bg-green-50 p-4 text-sm text-green-700">{message}</p>}{showForm && job && <div className="mt-8 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-5 flex items-center justify-between"><h2 className="text-2xl font-bold text-[#102A43]">Apply for {job.title}</h2><button type="button" onClick={() => setShowForm(false)} className="text-sm font-semibold text-slate-500">Cancel</button></div><JobApplicationForm job={job} onSubmit={apply}/></div>}</div>; }
